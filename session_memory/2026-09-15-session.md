# Agora — session memory, 2026-09-15 (Project Norm)

Room **Project Norm**, id `9c557ef5-4955-47c1-ae44-a6f1d3f17244`, orchestrator
megan-fox, crew optimus / bumblebee / ratchet / wheeljack / ironhide / soundwave.
Rules: `agents/_rules-project-norm.md`. All agent files untracked as of today.

---

## Dominic's decisions

1. **Build our own Norm from scratch.** Not an integration of Bland's Norm. I had
   proposed driving Bland's Norm through its public Triage API (see "Found" below);
   Dominic said: "gumawa ng bagong norm, hindi iimplement si norm."
2. **No teacher either.** Asked whether read-only `/status` runs of Bland's Norm
   could serve as a reference / answer key — Dominic: "Hindi, from scratch talaga."
   References are his screenshots, golden case #1 and the team's know-how.
3. ~~Brain = Cursor for now~~ — superseded by 5 the same evening.
4. **The product is named EVA.** Docs folder keeps its name.
5. **EVA's brain is the Claude CLI; its tools are EVA's own MCP server.** Came from
   Stephen ("maganda lang daw yong tools" — build an MCP, and DB tool calling that
   queries only what the question needs). Claude over Cursor because only the
   Claude CLI restricts tools per run (`--tools ""`, `--mcp-config` +
   `--strict-mcp-config`, `--allowedTools "mcp__eva__*"`), which is what makes "no
   shell, no .env, data only through MCP" real instead of a prompt rule. The Agora
   crew stays on Cursor. Agora's Claude driver already maps an agent's `mcp:` to
   `mcp__<id>__*` and passes `--mcp-config` (`claude-cli.ts:40-48`), but not
   `--strict-mcp-config`. Open with Stephen: semantic layer vs text-to-SQL with
   row-level security, MCP inside the backend or standalone, read replica / role.

All three are now written into the room rules under "Dominic's decisions".

---

## Phase 0 (run finished 06:59Z, 42 min) — what it established

- Gap table (Optimus [#75]): explain_schema, query_analytics **missing**;
  search_calls, open_call, open_pathway, dispatch_state, numbers_state **partial**;
  none exists.
- Ironhide FAIL: `call_queue_items` has `batchId` but no `client_id`
  (`packages/shared/src/schema.ts:3036-3067`) — querying by batch alone leaks tenants.
- Ratchet's schema cites spot-checked by me and correct: 3036, 3047, 3493, 3527
  (`pathwayLogs`), 3696 (`answeredBy`), 3704 (`transferTarget`), 4037, 4288.
- No research notes exist on disk: `/mnt/c/Projects/HelloAlex-Norm/` does not
  exist, so the access request auto-failed at [#2]. Everything lives in the
  transcript + mind stone.
- Golden case #1 root cause is still unknown.

## Found (context only — decision 1 and 2 rule out using it)

Bland exposes Norm publicly: Triage issues + `POST .../agent-sessions/{sid}/prompts`
with `/fix` (write, pathway edits), `/verify` and `/status` (read-only), and the
Activity feed streams agent `thought` / `action` / `response` / `artifact` events.
Bumblebee's "walang public API" was true only for Norm's internal tools.
Norm's public description: inspect live state → fork a draft → reproduce with Test
Bed replay → fix → verify; promotion is draft → commit → link.

---

## Mistakes worth not repeating

(a) **Optimus stalled 10 turns (#21–#62, ~30 of 42 min) because he runs in
Cursor's plan mode.** His tools (Read, Glob, Grep, WebSearch, WebFetch) are all in
`READ_ONLY_TOOLS` (`apps/server/src/drivers/cursor-cli.ts:53-61`), so the driver
passes `--mode plan` (`:96-97`). Plan mode answers with "Creating the … plan for
approval" instead of the deliverable — exactly what he repeated. He is the only
crew member in plan mode (the others hold Bash → full mode; Megan has no tools →
`--mode ask`) and the only one who stalled. **Confirmed by repro
and fixed:** a design prompt in plan mode put the whole deliverable inside a
`createPlanToolCall` and returned only "Delivering the design and gap table…" as
text; the same prompt in `--mode ask` returned it as text. The driver now sends
read-shaped agents to `--mode ask` (same CLI-enforced read-only boundary; the why
is in the file header). Web tools were rejected in BOTH modes — the global
`~/.cursor/cli-config.json` allows only `WebFetch(github.com)` and has
`autoAcceptWebSearch: false` — so no agent under the Cursor driver has working web,
whatever its `allow:` says. Left alone: that file is also Dominic's IDE config.
The same trap applies to a Cursor-brained Norm: read-only must not mean `--mode plan`.

(b) **Megan repeated the same nudge 10 times** without escalating or changing
approach — her Instructions are empty.

(c) **I misread the objective** as "use Bland's Norm". It is "copy Norm's
behaviour with our own harness".

---

## Open

- **Done today:** driver fix (a), typecheck clean; created
  `/mnt/c/Projects/HelloAlex-Norm/{research,design}`; posted the Phase 1 brief as a
  human message (~19:55 local). The run planned 6 steps, goal "Design doc under
  HelloAlex-Norm/design that can walk golden case #1 to the pathway node".
- **The brief's "i-Allow ko" was taken as a grant.** The planner gave all six crew
  `Write` + `Edit` and `add_dirs` on `/mnt/c/Projects/HelloAlex-Norm/design` —
  Ironhide and Soundwave included, who only audit and grade. With Write in their
  tools none of them counts as read-only, so the Cursor driver runs all six in full
  mode (the ask-mode fix does not reach them this run). Under Cursor that changes
  little: the five Bash holders were already in full mode and `add_dirs` is not
  enforced. Under the Claude driver it matters: Ironhide ("deliberately
  read-only") could now write. Decide whether to strip Write/Edit from Ironhide
  (and Soundwave) after the run. Lesson: a brief that says "I'll Allow it" is
  itself the grant — name the agents that need write.
- **Phase 1 closed** (17m 32s): architecture, playbook-v0, tool-contracts (with
  `hit_node_ids` for the transfer-intent signal), pathway-mount, Ironhide PASS on
  re-audit, Soundwave re-replay 5/5 ADOPT. All in `design/`.
- **Phase 2 posted (~20:25 local): EVA architecture research.** Before it:
  wrote `research/eva-mcp-sources.md` (MCP 2026-07-28 is stateless; Claude CLI
  `--strict-mcp-config`, `--max-turns`, `--max-budget-usd`, 25k-token MCP result
  cap; Postgres RLS bypass by owners/BYPASSRLS/default views) because the crew has
  no web under Cursor; rewrote Bumblebee as "EVA Research Scout" (the Bland-API
  lane is dead under decision 2); gave Ratchet EVA's MCP data layer; updated the
  lanes table. The planner granted all six READ on `research/` (add_dirs only, no
  new tools). Deliverable: `design/eva-architecture.md` + questions for Stephen.
- Cursor as a client-facing brain (subscription seat, rate limits shared with the
  IDE, runs on Dominic's PC) is fine for an internal prototype; revisit before
  anything reaches clients.
