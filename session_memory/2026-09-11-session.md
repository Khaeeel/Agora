# HelloAlex — session memory, 2026-09-10 → 11

Written so a fresh session (or Dominic after a reboot) can pick HelloAlex work up
without re-deriving anything. Newest work first inside each section. Agora runbook
and room setup stay in `2026-08-28-session.md` — do not overwrite that file.

**Tip:** `84ac6db93` on `origin/development`. Actions green:
https://github.com/KooyaPH/HelloAlexAI/actions/runs/34569486903

**Stephen tip under ours:** `6cbd8d9ff` Merge PR #222 `perf/enrichment-refill-pipeline`
(parent of `84ac6db93`).

---

## Voice Call Details — UUID / Default → real names

Lineage (newest first):

1. **`84ac6db93`** — harden voice-name resolve. Own-voice DB lookup is try/catch so a
   DB blip no longer aborts the whole resolve path; Bland catalog + Get Voice still
   run. `calls.ts` wraps resolve so a naming failure does not blank the Call Details
   panel. Tip of `development` as of this note.
2. **`28ce073bb`** — type Get Voice stubs in voice-name tests (unblocks development
   typecheck after the Bland fallback).
3. **`15056b5bb`** — resolve via Bland Get Voice. Dominic Test stores bare Bland
   UUIDs with no `client_voices` rows; catalog-only left raw ids on the UI. Strip
   `curated_` prefixes, map platform Alex id → Alex, match list id/`voice_id`, fall
   back to `GET /v1/voices/:id`. Also moved Phone Numbers **Analytics before
   Archived** so the clipped horizontal tab row still shows Analytics when rotation
   is off.
4. **`4caec17ce` / earlier `0fa5013c7`** — name Callisto / Pro-Team voices instead of
   **Default**. Migrated history often leaves `voice_id` null while
   `migration_provider_voice_id` still has the provider voice; Call Details only
   read `voice_id`. Resolve via migration voice (then pathway/task/Alex) when
   `callId` is available; pass `callId` on every `attachCallVoiceName` branch;
   match curated catalog ids including alternate Bland fields.

**Trap:** if own-voice DB errors are swallowed at the wrong layer, Call Details
shows a bare UUID again even though Bland would have named it. Harden at resolve
(try/catch + wrap) — do not "fix" by blanking the detail panel.

---

## Analytics / Phone Numbers (rotation entitlement)

Newest first:

- **`3188f9708`** — show shared Analytics when rotation entitlement is off. Do **not**
  blank the Analytics tab with the rotation-off empty state. Gate **only**
  rotation-specific panels (capacity, at-risk, today-vs-target, status). Keep shared
  number-usage metrics (e.g. most-used on calls).
- **`15056b5bb`** (also) — tab order **Analytics before Archived**.
- **`50c411f96` / `5818909a0`** — keep Analytics + Archived when rotation is off;
  hide **only** the Rotation Numbers tab. Batch Call draft TDZ fixed in the same
  wave (draft `useState` before the rotation autofill effect).

**Decision:** number-rotation entitlement gates rotation UI/charts, not the whole
Analytics surface. Treating Analytics as rotation-only was the regression.

---

## Other recent ships (Sep 10)

- **`3614bd587`** — rotation auto-fill pathway / pathway version / voice on select
  **and** on entitlement default preselect (no click). Hide the rotate picker when
  the account's narrowed `number-rotation` entitlementStates say hidden (same check
  as Phone Numbers → Rotation Numbers). Caller-id options APIs advertise
  `rotationEnabled` from `req.clientAuth.entitlementStates`. SMS campaigns stay
  `rotationEnabled: false`.
- **`5b3f6c9a4`** — empty area-code buy errors: clear copy instead of bare
  "Cannot buy".
- **`50c411f96` / `5818909a0`** — Batch Call draft TDZ: declaring draft after the
  rotation effect threw `ReferenceError` on mount.

---

## Process / open rules (HelloAlex room)

- **Lincoln:** ask-before-**every** push. No drive-by pushes, no "Actions will tell
  us". Local commit is fine; remote needs Dominic's explicit go (Lincoln owns the ask).
- **Mahone:** QA on-request only — do not open a QA sweep unless Dominic asks.
- **Never touch** Billing or Account Settings in the console.
- **No live Test Call / SMS** without Dominic on the call (or explicit go).

---

## Mistakes worth not repeating

(a) **Own-voice DB errors swallowed** → Call Details showed bare UUIDs. Resolve must
fall through to Bland catalog / Get Voice; naming failure must not blank the panel.

(b) **Analytics gated wholly on number-rotation entitlement** → accounts without
rotation lost shared Analytics. Gate rotation charts/tab only; keep Analytics +
Archived; Analytics before Archived in the tab row.

(c) **Committing unrelated docs-frontend dirt** with product fixes. Stage only what
the fix needs.

(d) **Pushing without Dominic's explicit go.** Ask-before-push is Lincoln's job;
agents do not "just push to unblock CI".

---

## Open / watch

- Tip `84ac6db93` is on `development` with Actions green for the harden commit —
  confirm any follow-up review comments on the voice-name path before stacking more
  Call Details work.
- Stephen's enrichment-refill merge (`6cbd8d9ff`) sits directly under the tip; avoid
  rewriting that history when branching for the next fix.
- Older Agora open items (verbatim WhatsApp relay, mind-stone structure, SMS timeout
  filter decision, Belick read access, leftover helloalex2 uncommitted support-ticket
  tree) remain in `2026-08-28-session.md` unless re-checked this week.
